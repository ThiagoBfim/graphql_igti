import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:graphql_igti/app/modules/new_course/new_course_page.dart';

void main() {
  testWidgets('NewCoursePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewCoursePage(title: 'NewCourse')));
    //  final titleFinder = find.text('NewCourse');
    //  expect(titleFinder, findsOneWidget);
  });
}
